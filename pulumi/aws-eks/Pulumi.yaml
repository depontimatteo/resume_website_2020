name: resume-website
runtime:
  name: python
  options:
    virtualenv: ./.venv
description: Depo Resume Website
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: eu-south-1
